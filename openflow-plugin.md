# OpenFlow 浏览器插件使用指南

欢迎使用 **OpenFlow 数据提取插件**。本指南将帮助您快速了解并上手使用本插件，高效、准确地从工单页面提取素材需求数据，并与我们的桌面端应用无缝衔接。

---

## 📖 目录
1. [快速使用](#1-快速使用)
2. [功能详解与设置](#2-功能详解与设置)
3. [如何搭配桌面端应用使用](#3-如何搭配桌面端应用使用)
4. [常见问题 (FAQ)](#4-常见问题-faq)
5. [温馨提示与免责声明](#5-温馨提示与免责声明)

---

## 1. 快速使用

只需简单的几个步骤，您就可以完成页面的需求数据提取与下载。

### 1.1 打开目标单据页面
请在浏览器中登录系统，并打开您需要抓取数据的工单/单据详情页面。
> **注意**：请务必等待页面完全加载完毕，确保相关的单据信息、规格和文本描述均已显示。

### 1.2 唤起 OpenFlow 插件
点击浏览器右上角扩展程序栏中的 **OpenFlow 插件图标**（如果是初次安装，请在“拼图”图标中找到它并将其固定在工具栏）。
此时会弹出插件的控制台主界面。

![插件主界面](插件主界面.png)

### 1.3 一键提取数据
在插件界面中，点击 **“📥 提取当前页面任务”** 按钮。
插件会自动扫描当前网页，抓取项目名称、素材类型、尺寸需求等核心数据。
提取成功后，您将在界面的状态区看到抓取结果和识别到的任务列表。

![数据提取结果](数据提取结果.png)

### 1.4 导出数据文件
确认抓取到的任务列表信息无误后，您可以在底部选择导出格式：
- **📊 导出 Excel**：生成干净、规范的数据报表（可自动区分并适配“平面”或“视频”的定制模板）。
- **📋 导出 JSON**：生成结构化的 JSON 数据文件，方便后续用于程序化处理或导入桌面端应用。

点击下载后，文件将以提取到的“项目名称”自动命名，并保存到您的浏览器默认下载目录中。

![导出功能区](导出功能区.png)

---

## 2. 功能详解与设置

OpenFlow 插件为您提供了个性化的配置选项，以适应不同的报表输出需求。

### 主题切换
点击界面右上角的 **“☀️ / 🌙”** 按钮，可以快速在明亮模式和暗黑模式之间切换，以适应不同的系统偏好或光线环境。

### 自定义报表表头设置
如果您需要修改导出的 Excel 报表的列名（表头），可以点击右上角的 **“⚙️ 设置”** 按钮打开设置面板。

在设置面板中，您可以分别配置：
- **平面报表顶部字段**（逗号分隔）
- **视频报表顶部字段**（逗号分隔）

配置完成后，点击 **“保存设置”**。下次点击导出 Excel 时，系统将应用您刚刚设置的最新的表头结构。

![设置面板](设置面板.png)

---

## 3. 如何搭配桌面端应用使用

OpenFlow 插件不仅仅是一个单纯的数据抓取工具，它生成的标准化数据是驱动整个自动化工作流的“核心引擎”。通过与 **OpenFlow 桌面端应用** (OpenFlow Desktop) 配合，您可以实现从需求提取到素材交付的全流程自动化。

### 推荐工作流：

1. **需求提取阶段 (浏览器端)**：
   - 按照前文所述，在浏览器工单页面中使用 OpenFlow 插件提取数据。
   - 点击 **“📋 导出 JSON”** 将需求数据保存为本地 JSON 文件（推荐）。

2. **准备工作目录 (本地)**：
   - 将导出的 JSON 文件放置在一个专属的工作文件夹中。
   - 将设计师制作好的相关图片、视频素材文件也放入这个工作文件夹中。

3. **校验与归档阶段 (桌面端)**：
   - 打开 **OpenFlow 桌面端应用**。
   - 在应用中导入该工作文件夹。
   - 桌面端应用会自动读取插件生成的 JSON 需求清单，并与本地实际存在的媒体素材进行**智能比对**。
   - 它可以自动验证素材的**尺寸分辨率**、**文件格式**、**文件大小**等是否严格符合工单要求，并在验证通过后，按照规范自动为文件重命名。

通过这种“端到端”的配合，您再也不用人工逐一核对素材规格与繁杂的重命名工作了！

---

## 4. 常见问题 (FAQ)

**Q1：点击“提取”后，提示找不到有效任务或数据为空？**
- A：请检查网页内容是否已经彻底加载完毕（特别是左侧/右侧边栏数据加载较慢时）。如果依然失败，请确认您所处的是支持数据提取的目标工单页面，而非主页或列表页。

**Q2：导出的 Excel 文件里，有些列没有数据？**
- A：插件会基于网页上的可见信息及规则进行尽力抓取（例如寻找制作人、集团名称等特定键值对）。如果页面本身没有填写该项信息，导出的文件中对应单元格将留空。

**Q3：我能自己调整导出的字段顺序吗？**
- A：目前 Excel 表头名称支持通过点击右上角“⚙️设置”进行自定义修改。但深度定制的字段映射或数据排序逻辑内嵌在代码中，若需调整，请联系开发人员或查阅 `README.md` 进行二次开发。

**Q4：JSON 和 Excel 我该导出哪一个？**
- A：如果您是要给人看，或者需要人工统计核对，请导出 **Excel**；如果您是要将数据导入到我们的“桌面端应用”进行程序化的素材校验，请导出 **JSON**。

---

## 5. 温馨提示与免责声明

### 💡 温馨提示
- **常驻工具栏**：为了随时方便提取，强烈建议您在 Chrome 扩展管理界面将本插件**固定 (Pin)** 在浏览器顶部的工具栏中。
- **文件命名覆盖**：插件会自动使用抓取到的“项目名称”来重命名导出的文件。如果在同一个文件夹内重复下载同一项目的多次提取，可能会产生名称冲突或生成带 `(1)` 后缀的文件，请注意文件管理。

### ⚠️ 免责声明
1. **数据隐私**：本插件完全运行在您的**本地浏览器**环境中，提取的所有数据均在本地处理并直接生成文件下载。本插件**绝不会**主动收集、上传您的任何业务数据至任何第三方服务器。
2. **正确性责任**：本插件旨在通过自动化手段减轻人工复制粘贴的负担并提升效率。由于源网页 DOM 结构可能发生变动或出现非标准输入，本插件无法做出 100% 精准的保证。**用户在交付关键数据前，仍需对导出的结果文件承担最终核对责任。**
3. **软件性质**：本工具按“原样”提供。开发者对因使用本工具引起的任何直接、间接或连带的业务损失不承担任何法律责任。
